# Why plskit?

> Status: placeholder. Content TBD.

The pitch — when to reach for `plskit` vs `scikit-learn`'s `PLSRegression`,
the R `pls` package, `pyls`, or `pyChemometrics`. What this library does
that the others don't, and what they do that this library deliberately
doesn't.
