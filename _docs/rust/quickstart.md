# Quickstart

> Status: stub. Content TBD.

- Minimal example: build `X`, `y` as `faer::Mat`, call `pls1_fit`, read the result
- Confirmatory test path with `pls1_confirmatory_test`
- Exploratory CV path with `pls1_cv`
- Side-by-side comparison with the equivalent Python snippet
- Pointer to docs.rs/plskit for the full API reference
