# Architecture

> Status: stub. Content TBD.

- Top-level shape of the `plskit-rs` crate: modules, public surface, internal helpers
- The 3-layer result-object architecture as it appears to contributors (Rust core ↔ wrapper ↔ user)
- Where the Rust ↔ Python boundary sits and how PyO3 bindings are wired
