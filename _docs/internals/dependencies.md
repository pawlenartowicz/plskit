# Dependencies

> Status: stub. Content TBD.

- Why `faer` is the linear-algebra backbone (vs `nalgebra`, `ndarray-linalg`)
- RNG choice and seeding discipline
- Procrustes alignment as a separate crate (`procrustes`)
- Build-time vs run-time dependencies; what wrappers re-export vs hide
