# Quickstart

> Status: stub. Content TBD.

- Minimal end-to-end example: load data, call `pls1_fit`, inspect the `PLS1Result`
- Confirmatory test in three lines: `pls1_confirmatory_test(X, y, k=1)`
- Exploratory CV in three lines: `pls1_cv(X, y, k_max=10)`
- Plotting scores and loadings via the optional `[plot]` extra
- Pointer to the API reference and result-object docs for the next step
